<%*
// 1. 彈出視窗詢問檔案名稱
let itemName = await tp.system.prompt("請輸入新物品的名稱 (中文名)：");
let output = await tp.system.prompt("請輸入該物品一個配方能做出的數量");
// 2. 動態讀取資料夾路徑
let folderList = app.vault.getAllLoadedFiles()
    .filter(f => f instanceof tp.obsidian.TFolder && f.path !== "/")
    .map(f => f.path);

// 3. 彈出選單，並在搜尋框帶入提示文字
let selectedFolder = await tp.system.suggester(
    folderList, 
    folderList, 
    false, 
    "請選擇要將此物品創建在哪個資料夾..."
);

// 4. 執行重新命名與移動
if (itemName && selectedFolder) {
    await tp.file.rename(itemName);
    await tp.file.move(selectedFolder + "/" + itemName);
} else {
    // 如果中途按 ESC 取消，則停止執行後續操作
    return;
}
_%>
---
type: sf-item
id: 尚未輸入
output: <%output%>
aliases:
---

# 合成/取得
### 原版

### 液體

### 無限附加

### 煉金術自傳-煉金術

# 用途
- 

# 產物
- 
